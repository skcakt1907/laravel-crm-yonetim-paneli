<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true">
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>

    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>

    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="CACHE_STORE" value="array"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="TELESCOPE_ENABLED" value="false"/>

        <!--
            VERİTABANI: ayrı bir test veritabanı YOK, geliştirme veritabanı
            kullanılıyor. Bu bilinçli bir tercih:

            Bu projenin migration geçmişi canlı veritabanıyla senkron değil
            (canlıdan kopyalanan bir şema üzerinde çalışıyoruz), bu yüzden
            "migrate:fresh ile temiz test DB'si kur" çalışmıyor.

            Bunun yerine testler DatabaseTransactions kullanır: her test kendi
            işleminde çalışır ve sonunda geri alınır.

            DİKKAT — RefreshDatabase ASLA KULLANILMAMALI. Bu veritabanı canlının
            kopyası; RefreshDatabase onu siler.

            DİKKAT 2 — Rollback yalnızca InnoDB tablolarda çalışır. `yoneticiler`,
            `roller`, `gunluk_gorev_log` MyISAM: bunlara YAZAN test, yazdığını
            kendisi temizlemek zorundadır (bkz. SosyalMedyaRaporTest).
        -->
    </php>
</phpunit>
